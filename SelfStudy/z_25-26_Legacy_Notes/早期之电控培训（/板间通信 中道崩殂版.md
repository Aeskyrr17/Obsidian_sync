---

---
`硬件收到数据` -> `触发中断` -> `进入Callback` -> `判断StdId（是谁发的）` -> `解析数据（翻译内容）` -> `存入全局变量或发布话题`。
底盘跟云台，角度判断

```c++
RelativeAngle = board_comm.target_yaw;
            if (RelativeAngle > Pi)
            {
                RelativeAngle -= 2*Pi;
            }
            else if (RelativeAngle < -Pi)
            {
                RelativeAngle += 2*Pi;
            }

```

- 大部分电机（请看具体代码！！！）把fdb限制在 -pi ~ pi
	- 当需要多圈转动的时候，会出现 -Pi和Pi的边界跳动
```cpp
ref_pos = target_pos;
rnt_pos = motor_pos_fdb;
在普通pid算法中： err = ref - fdb  <-- 会导致跳变
所以用取模，把角度限制在 -Pi 到 Pi
shortest_err = LoopfloatConstrain()

在具体实现pid的时候
pid_ref = ref_pos;
fdb = fdb - shortest_err;
```